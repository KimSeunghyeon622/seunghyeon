-- =====================================================
-- 상품 이미지 업로드 기능을 위한 테이블 수정
-- =====================================================

-- 1단계: products 테이블에 image_url 컬럼 추가
-- =====================================================

ALTER TABLE products
ADD COLUMN IF NOT EXISTS image_url TEXT;

-- 2단계: 코멘트 추가 (문서화)
-- =====================================================
COMMENT ON COLUMN products.image_url IS '상품 이미지 URL (Supabase Storage)';

-- =====================================================
-- 완료!
-- =====================================================
-- 이제 상품 등록/수정 시 이미지를 업로드할 수 있습니다.
-- Storage 버킷: store-documents
-- 경로: product-images/
-- =====================================================
